# utils/models_vision.py

VISION_OCR_MODELS = [
    # =========================================================
    # 【無料枠】（Vision OCR 用 API モデル）
    # =========================================================

    # ---------------------------------------------------------
    # OpenAI — gpt-4o-mini（Vision 対応）
    # ---------------------------------------------------------
    {
        "provider": "openai",
        "model": "gpt-4o-mini",
        # パラメータ: 非公開（軽量系）
        # 特徴: Vision 対応 / 高速 / 無料枠あり
        # 用途: 画像からの OCR / 構造化抽出
    },

    # ---------------------------------------------------------
    # Anthropic — Claude Haiku（Vision 対応）
    # ---------------------------------------------------------
    {
        "provider": "anthropic",
        "model": "claude-haiku-4-5-20251001",
        # パラメータ: 非公開（軽量系）
        # 特徴: Vision 対応 / 無料枠あり
        # 用途: OCR 後の補正・構造化 / 表理解
    },

    # ---------------------------------------------------------
    # Google — Gemini 2.5 Flash Lite（Vision 対応）
    # ---------------------------------------------------------
    {
        "provider": "google",
        "model": "gemini-2.5-flash-lite",
        # パラメータ: 非公開（軽量系）
        # 無料枠: 1,000 RPD（最も広い）
        # 特徴: 高速 / 大量処理向き / Vision 対応
        # 用途: OCR 補助・軽量構造化
    },
]

# =========================================================
# 【有料候補】（コメントアウトで保持 / accidental charges 防止）
# =========================================================

# --- OpenAI ---
# {
#     "provider": "openai",
#     "model": "gpt-4.1",
#     # パラメータ: 非公開（大型）
#     # 特徴: Vision 高精度
# },

# --- Anthropic ---
# {
#     "provider": "anthropic",
#     "model": "claude-sonnet-4-6-20250514",
#     # パラメータ: 非公開（中〜大型）
#     # 特徴: Vision 高精度
# },

# --- Google ---
# {
#     "provider": "google",
#     "model": "gemini-2.5-pro",
#     # パラメータ: 非公開（大型）
#     # 特徴: Vision 最高精度
# },
